# 更新日誌 Changelog

## v1.8.5 — 修正切畫面未回頂端

- 進入房間等可能停在畫面中段（手機輸入名稱後鍵盤收合殘留偏移）。`show()` 改為切畫面時主動 blur 關鍵盤，並於當下與下一幀把 window/body/documentElement 一併捲回頂端。

## v1.8.4 — 開放單人開局（空角自動補 Bot）

- **移除「至少 2 位」開局限制**：只要有 1 位真人並選好角色即可開始多人房。
- **開局時自動為空缺角色補 Bot**：`startGame` 為沒有玩家的角色加入電腦玩家，確保上下游皆有 AI 波動（Bot 同受預算上限約束）。
- 前端開始鈕提示改為「空缺角色將由電腦自動接手，可直接開始」。
- 經 e2e 驗證：單人(0 Bot)可開局 → 自動補 3 Bot → 完整跑完 20 週、四角色歷史齊全、Bot 訂單有波動。

## v1.8.3 — 文案校正與一致化

### 修正（文字與實際機制對齊）
- **製造商角色卡**：移除不存在的「生產線數量」機制描述，改為「在製品（在途生產）」，提示文字同步更新。
- **單人需求模式分類**：原判斷順序使季節型／自然波動型永遠觸發不到（單人實際只會出現 2 種）。重寫為與伺服器一致的 5 種（三重／雙重／夏季／冬季／突發），emoji 與描述對齊；經 2000 局驗證 5 種皆可出現。
- **複盤評語**：移除 −$1000 下限後帳戶可為負，「帳戶歸零！破產！」改為「帳戶見底，負債累累！」。
- 修正 `genDemand` 建議公式註解（×1.2 → ×1.5，與實際程式一致）。

### 文案優化
- 大廳標語「賣出越多賺越多」→「賣得掉才賺錢，囤貨和缺貨都燒錢！」（避免暗示超量下單有利）。
- 帳戶說明「進帳 +$賣價」→「依角色售價進帳」。
- 在途標籤「在途（LT 2週）」→「在途（下單後2週到）」。
- 規則面板補上關鍵設定「期初庫存 12 箱，前 2 週無到貨」（房間版與單人版皆補）。
- README 需求模式 emoji 與程式一致。

## v1.8.2 — 帳戶誠實化 + 伺服器端防惡意拉貨

### 調整
- **移除 −$1000 帳戶下限**：原本把顯示現金夾在 −$1000，對玩法無作用（破產機制其實來自「可訂量 = 現金/進價」），卻會在多人累加時汙染數值、並在複盤低估牛鞭效應造成的真實虧損。移除後帳戶誠實反映盈虧。

### 安全
- **伺服器端預算上限**：`submitOrder` 新增 `ORDER_EXCEEDS_BUDGET` 檢查，從 `weeklyHistory` 即時算出現金，拒絕 `quantity > floor(現金/進價)` 的下單。原本此上限只在前端強制，惡意玩家可繞過 UI 直接送超大訂單拉爆上游；現已於後端把關（規則與前端 `getMaxOrder` 一致）。
- **Bot 下單同受預算上限**：`resolveWeek` 將 bot 的 `calcBotOrder` 結果以 `maxAffordableOrder` 夾住，與真人玩家規則一致，避免 bot 越界下單。
- `gameConfig.js` 新增 `START_CAPITAL`、`SELL_PRICE`、`BUY_PRICE`（與前端一致），作為伺服器帳戶/預算計算來源。

### 修正
- 多人提交失敗時 `submitting` 未重設導致無法再次提交的問題；並為預算超限顯示友善訊息「資金不足！最多可訂 N 箱」。

## v1.8.1 — 清理與多人模式標示

### 清理
- **移除廢棄的 `client/` React 版前端**：該版為半成品（多個元件為空檔、欄位與後端 `week_results` 不符），且從未部署。實際部署前端為 `docs/beer_game_demo.html`。如需 React 版可從 git 歷史還原。
- **移除已脫鉤的 server 端勝負/排名死碼**：`calculateResults` 原算的 `teamWin`（門檻 `< 700`，舊成本模型殘留，實際幾乎不可能達成）、`teamTotal`、`rank`、`shortageWeeks`、`orderSD` 從未被現行前端讀取——前端改用帳戶/利潤模型自 `fullHistory` 重算。一併移除 `gameConfig.js` 中全專案未引用的 `EXCELLENT_THRESHOLD`。`calculateResults` 現只回傳前端真正使用的 `demandInfo`。

### 新增功能
- **多人房間模式橫幅**：等待室頂部顯示「👥 多人連線模式」，並依身分標示「👑 房主」或「🙋 玩家」，與單人模式標籤一致。

## v1.8.0 — 遊戲平衡重設計・UI 精簡

### 平衡調整
- **毛利率梯度修正**：零售 40% > 批發 35% > 分銷 29% > 製造 20%（原本反向，製造毛利最高）
- **持有成本修正**：零售 $3 > 批發/分銷 $2 > 製造 $1（原本反向，製造最高）
- **缺貨罰統一比例**：所有角色缺貨罰 = 持有費 × 2（1:2 比例，原各角色比例不一）
- **期初在途設為 [0, 0]**（原 [4, 4]），第 1-2 週無到貨，增加初期決策挑戰

### 新增功能
- **等待室「規則說明」面板**：可展開/收合，顯示完整成本表
- **成本表動態生成**：`renderCostTables()` 從 JS 常數生成，修改價格一處即全部同步
- **各角色任務文字**：改用 template literals 引用常數，不再有硬編碼錯誤數字

### UI 調整
- 大廳頁加入遊戲介紹文字（原在單人選角頁）
- 單人選角頁改為簡潔「🎮 單人模式」標籤
- 移除最高帳戶紀錄排行榜
- 積壓標籤：「積壓欠單 / 箱未出貨」
- 在途標籤：「在途（LT 2週）」

### 文件更新
- `docs/GAMEPLAY.md`：補充各角色定價、成本表、設計邏輯說明
- `docs/ARCHITECTURE.md`：修正檔案路徑（demo/ → docs/）、管道初始值
- `CLAUDE_CODE_HANDOFF.md`：同步所有定價與架構資訊

---

## v1.7.0 — 多人凍結修復・穩定性強化・GitHub Pages 部署

### 修復
- **多人模式凍結 bug（致命）**：第 2 週後送出訂單會永久卡住，無法進入下一回合
  - 根本原因：`submission_progress` 事件覆寫 `g-wait` innerHTML，導致 `g-sub-n` span 消失；
    第 2 週 `doSubmit` 嘗試讀取 `g-sub-n` 拋出 TypeError，`socket.emit` 從未執行，伺服器收不到訂單
  - 修法：`doSubmit` 自行重建整個 `g-wait` innerHTML（含新 `g-sub-n` span），不再依賴預存 DOM 狀態
  - 驗證：本地測試 1 人 + 3 Bot，完整跑完 20 週無凍結
- **NotoColorEmoji 404 錯誤**：移除不存在的 CDN 字型宣告，改用系統 emoji
- **Health endpoint CORS 錯誤**：後端 Express 加入 `Access-Control-Allow-Origin: *` middleware

### 新增
- 伺服器端 `resolveWeek()` 加入 try-catch，錯誤時 emit `server_error` 給前端
- 前端 `server_error` handler：自動解凍 UI（顯示動作區，清除等待畫面）
- `demo/index.html`：GitHub Pages 進入點，自動重導向至 `beer_game_demo.html`

### 改善
- 前端部署從 Netlify 遷移至 **GitHub Pages**（免費、無配額限制）
  - 網址：`https://royhsu1012.github.io/beer-game/`

---

## v1.6.0 — Pixel Art 精緻化・中文字體・需求公式升級

### 新增
- 🔤 採用 **Cubic 11** 開源中文 Pixel Art 字體（支援完整繁體中文）
- 🎭 角色像素圖從 16×16 升級至 **20×24 格**，細節大幅提升：
  - 零售商：藍帽四層漸層、腰帶扣環、三層鞋底
  - 批發商：綠安全帽帽檐、腰帶黃金扣、安全靴
  - 分銷商：棕色頭髮、雙色墨鏡鏡片反光、黃夾克陰影層
  - 製造商：紅帽四層、棕色鬍鬚紋理、扳手細節
- 🎲 需求曲線升級為**週期性隨機公式**：
  - 主週期正弦波（隨機振幅、週期、相位）
  - 次諧波（增加自然感）
  - 線性趨勢（微弱上升或下降）
  - 白噪音（每週小幅隨機）
  - 突發衝擊事件（0-2個，隨機時間點和幅度）
- 🌀 自動識別五種需求模式：夏季旺季、冬季旺季、雙重衝擊、突發衝擊、自然波動
- CRT 螢幕效果（掃描線 + 邊緣暈影）
- 複盤圖表標記需求峰值點

### 改善
- 動作區綠色發光邊框更明顯
- 快速選鈕顯示系統建議下單量
- 計時器進度條像素風格
- 複盤牛鞭效應圖加入網格線和發光效果

---

## v1.5.0 — 繁體中文介面・清晰 UX

### 新增
- 全介面改為繁體中文
- 每個畫面只有一個焦點動作
- 角色介紹頁（能看到什麼 / 看不到什麼 / 勝利條件）
- 快速選鈕（建議下單量 / 4箱 / 0箱）
- 本地最佳紀錄排行榜

### 改善
- 三大數字（庫存 / 到貨 / 需求）大字顯示
- 庫存不足時自動變色警示
- 上週成本明細展開顯示
- 每週動態提示

---

## v1.4.0 — 動態季節性需求・Pixel Art 角色

### 新增
- 四種隨機需求模式（開局不告知，複盤才揭曉）
- Canvas 繪製像素角色（16×16 格）
- 四角色浮動動畫
- CRT 掃描線效果

---

## v1.3.0 — 遊戲引擎完整

### 新增
- 完整 20 週遊戲流程
- WebSocket 實時四人多人對戰
- 資訊隔離（服務器端保證）
- 每週 60 秒計時器
- Chart.js 歷史圖表
- 複盤頁面（牛鞭效應全景圖）

---

## v1.2.0 — 後端架構

### 新增
- Node.js + Express + Socket.io 後端
- 記憶體房間管理（無需資料庫）
- GameEngine.js 純函數設計
- RoomManager.js 房間生命週期管理

---

## v1.0.0 — 初始版本

- React + Vite 前端架構
- 四個角色設計
- 基礎遊戲邏輯
